# FastAPI Fashion Store

This is a backend project built using FastAPI as part of FastAPI internship final project submission.

## Features
- GET APIs
- POST APIs with Pydantic validation
- Helper functions
- CRUD operations
- Multi-step cart and checkout workflow
- Search, sorting, and pagination
- Swagger API testing

## Run project
pip install -r requirements.txt
uvicorn main:app --reload
